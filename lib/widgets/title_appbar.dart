/*

class TitleAppbar extends StatelessWidget {
  final CuentaNextcloud cuenta;
  final String title;

  const TitleAppbar({super.key, required this.cuenta, required this.title});

  @override
  Widget build(BuildContext context) {
    return Row(
      mainAxisAlignment: .start,
      children: [
        CuentaAvatar(cuenta: cuenta, size: 30),
        const SizedBox(width: 10),
        Text(title),
      ],
    );
  }
}
*/
